# 可复用构建思路

最后更新：2026-04-01
用途：本文件抽象当前项目里可迁移到其他项目的方法论，只继承构建思路、协作结构和演进方式，不继承当前业务名词、页面设计或具体接口。

## 1. 核心原则

### 1.1 先建立“文档入口”，再开始持续开发

这个项目最值得复用的不是某个具体页面，而是先把“续接入口”做清楚：

- 根目录只保留一个完整主文档
- 其他文档按状态、结构、规则、专项任务分层
- 新人或新智能体接手时，先读文档，再动代码

可迁移价值：

- 降低换人成本
- 降低多智能体协作时的信息丢失
- 避免重要事实只存在于聊天记录

### 1.2 只认“已验证主链路”，不认想象中的完整产品

当前项目一直强调：先确认真正能跑通的链路，再讨论扩展能力。

可迁移做法：

- 先定义一条用户可见、可复验的主链路
- 所有新功能都说明自己属于：已验证、部分接入、仅规划
- 不把“代码骨架”表述成“功能已完成”

### 1.3 主线与实验线分离

这个项目同时保留了：

- `unifocus/web/`：主业务前端
- `unifocus/web-vite/`：实验前端

可迁移的方法不是“一定要双前端”，而是：

- 稳定主线不要被实验直接冲掉
- 需要大胆探索时，给实验单独边界
- 实验成熟前，不要提前宣称它已经替代主线

### 1.4 先修可访问性，再做美化和重构

当前前端修复过程沉淀出的顺序很值得复用：

1. 先确认文档写的入口
2. 再确认真实路由
3. 再确认鉴权跳转
4. 再确认运行态和构建产物
5. 再确认接口返回数据形状
6. 最后才做重构、去重和美化

这个顺序的本质是：

- 先恢复系统可达
- 再恢复系统稳定
- 最后提升系统质量

### 1.5 生成物不算项目事实

这个项目明确把这些东西排除在“真实结构”之外：

- `node_modules`
- `.next`
- `.gocache`
- `.venv`

可迁移的核心思路是：

- 把源码、配置模板、脚本、文档当作项目事实
- 把缓存、构建产物、依赖目录当作可再生结果
- 文档与结构分析时，不让生成物干扰判断

## 2. 可迁移的项目骨架

其他项目如果要借用本项目思路，可以继承下面这套抽象骨架：

### 2.1 根层结构

- `agent.md`
  - 完整主入口
- `AGENTS.md`
  - 兼容入口
- `docs/`
  - 统一文档根目录
- `app/` 或主源码目录
  - 真正的代码实现
- `.env.example`
  - 环境模板
- `scripts/`
  - 本地开发辅助脚本

### 2.2 文档分层

建议至少保留这几类文档：

- `docs/project/current-status-and-direction.md`
  - 最近一轮工作完成了什么，下一步做什么
- `docs/project/project-memory.md`
  - 长期有效的决策、边界、环境特例
- `docs/project/baseline.md`
  - 当前真实可运行基线
- `docs/project/structure.md`
  - 目录结构与真实入口
- `docs/project/agent-execution-rules.md`
  - 协作与执行规则
- `docs/.../handoff-or-task-doc.md`
  - 给下一位执行者的专项交接

### 2.3 代码层分线

如果项目复杂度足够，可以按“能力线”组织，而不是按聊天顺序组织：

- 后端主线
- 前端主线
- 实验线
- 可选的 AI / NLP / 数据处理能力线

关键不是目录长什么样，而是每一条线都要回答：

- 它是不是主线
- 它现在是不是必须启动
- 它和其他线的边界是什么

## 3. 其他项目如何继承

### 3.1 第一步：复制方法，不复制名字

其他项目不要照抄这些具体词：

- `Unifocus-v1.2`
- `dashboard`
- `planet`
- `opportunity`
- `competition`

真正该继承的是：

- 一个完整主入口
- 一套分层文档
- 一条已验证主链路
- 一套主线 / 实验线边界
- 一套交接标准

### 3.2 第二步：先定义一条最小主链路

新项目建议最先写清楚：

- 用户现在能访问哪个地址或命令
- 这个入口背后会经过哪几层
- 哪些服务必须启动
- 哪些能力当前不是阻塞项

这样后续任何人接手，都不会一上来就误以为“所有模块都要一起跑”。

### 3.3 第三步：把“当前状态”与“长期记忆”拆开

这是这个项目里非常值得复用的一点。

不要把这些信息混在一份文档里：

- 当前这一轮刚发生了什么
- 长期都有效的架构边界是什么

建议拆成：

- `current-status-and-direction.md`
- `project-memory.md`

这样可以同时保证：

- 当前进展可续接
- 长期规则不被短期任务冲掉

### 3.4 第四步：为实验保留空间，但不要污染主线

如果其他项目也要尝试新框架、新 UI、新交互，可以照搬这个思路：

- 给实验单独目录
- 给实验单独文档
- 明确它只是实验，不是默认主入口
- 等实验成熟后，再决定是否迁移

### 3.5 第五步：把排障顺序固化下来

本项目最可复用的经验之一，就是把排障顺序写进文档，而不是临时靠经验回忆。

推荐抽象成通用顺序：

1. 文档入口
2. 真实访问入口
3. 鉴权与跳转
4. 运行进程与构建产物
5. 数据形状
6. 重构与美化

这个顺序适用于大多数带前后端和多环境的项目。

### 3.6 第六步：强制交接最小格式

其他项目也建议固定最小交接模板：

1. 当前现象
2. 已确认根因
3. 已修改文件
4. 已验证结果
5. 剩余风险
6. 下一步建议

这样即使跨人、跨会话、跨智能体，接力成本也会低很多。

## 4. 不建议直接继承的部分

其他项目不应该直接复制这些内容：

- 当前业务命名
- 当前页面结构
- 当前端口与数据库账号
- 当前前端技术选型细节
- 当前具体的 Dashboard / Planet 视觉表达

原因很简单：

- 这些是当前项目的业务实现
- 不是方法论本身
- 直接照搬会让新项目背上不必要的历史包袱

## 5. 最小落地模板

如果要把这套思路迁移到新项目，最少做这 8 件事：

1. 定义仓库主名称与源码主目录名称
2. 建立 `agent.md` 作为完整主入口
3. 建立 `docs/project/` 下的状态、记忆、基线、结构文档
4. 明确一条已验证主链路
5. 明确哪些模块当前不是阻塞项
6. 如有实验方向，为其单独设边界
7. 固化排障顺序与交接模板
8. 把环境模板、启动脚本、忽略规则和生成物边界写清楚

## 6. 一句话总结

其他项目真正应该继承的，不是 Unifocus 的业务外壳，而是这套“文档先行、主线稳定、实验隔离、按真实链路推进、把排障与交接写进结构里”的构建方式。
